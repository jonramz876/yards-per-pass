pandas==2.2.3
pyarrow==18.1.0
# psycopg2-binary bundles libpq — no system PostgreSQL install needed.
# Fine for CI/scripting; production web servers should use psycopg2 with system libpq.
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pytest==8.3.4
